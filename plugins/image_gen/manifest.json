{
    "tag": "IMAGE_GEN",
    "description": "Generates images from text descriptions using AI image models.",
    "lazy_load": true,
    "is_class_based": true,
    "status": "ONLINE",
    "commands": {
        "generate_image": "Generates an image from a text description. Uses the configured provider."
    },
    "tool_schema": [
        {
            "type": "function",
            "function": {
                "name": "IMAGE_GEN__generate_image",
                "description": "Generates an image from a text description. Uses the configured provider.",
                "parameters": {
                    "type": "object",
                    "properties": {
                        "prompt": {
                            "type": "string",
                            "description": "Detailed description of the image to generate."
                        }
                    },
                    "required": [
                        "prompt"
                    ]
                }
            }
        }
    ]
}